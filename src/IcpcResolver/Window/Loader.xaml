<Window x:Class="IcpcResolver.Window.Loader"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        mc:Ignorable="d"
        Title="Icpc Resolver Loader" Height="100" Width="370">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="150"/>
        </Style>
    </Window.Resources>

    <DockPanel Margin="15 15 15 15">
        <Button DockPanel.Dock="Left" Content="Select A Config File" Click="LoadConfig_OnClick"/>
        <Label Content="Or" VerticalContentAlignment="Center"/>
        <Button DockPanel.Dock="Right" Content="Generate A Config File" Click="GenerateConfig_OnClick"/>
    </DockPanel>
</Window>
