<Window x:Class="IcpcResolver.Net.Window.Resolver"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IcpcResolver.Net"
        xmlns:userControl="clr-namespace:IcpcResolver.Net.UserControl"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowState="Maximized" WindowStyle="None">
    <!-- style -->
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <!-- main window -->
    <StackPanel Background="Gray">
        <!-- header -->
        <Border BorderThickness="0 0 0 3" BorderBrush="White">
            <Grid Background="Black">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- rank -->
                <Label Grid.Column="0"
                       HorizontalContentAlignment="Left"
                       Content="Rank"/>
                
                <!-- display name -->
                <Label Grid.Column="1"
                       Margin="37 0 10 0"
                       HorizontalContentAlignment="Left"
                       Content="Name"/>
                
                <!-- solved -->
                <Label Grid.Column="2"
                       HorizontalContentAlignment="Right"
                       Content="Solved"/>
                
                <!-- time -->
                <Label Grid.Column="3"
                       HorizontalContentAlignment="Right"
                       Margin="10 0 20 0"
                       Content="Time"/>
            </Grid>
        </Border>

        <!-- content -->
        <Grid x:Name="Teams"/>
    </StackPanel>
</Window>
